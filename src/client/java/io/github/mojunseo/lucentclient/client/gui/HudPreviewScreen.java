package io.github.mojunseo.lucentclient.client.gui;

/**
 * Marks screens that show HUD modules for editing. While one is open, modules with nothing to show
 * (no armor, no effects, ...) draw sample content so they can still be placed and styled.
 */
public interface HudPreviewScreen {
}
